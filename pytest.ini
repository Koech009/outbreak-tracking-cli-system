# Configuration for pytest test discovery and behavior

[pytest]
testpaths = tests
python_files = test_*.py

filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning

addopts = -v --tb=short --disable-warnings
